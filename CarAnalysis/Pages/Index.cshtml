@page
@model CarAnalysis.Pages.IndexModel
@{
    ViewData["Title"] = "صفحه اصلی";
}

<!-- find section -->
<section class="find_section layout_padding2">
    <div class="container">
        <form method="get" asp-page="./Search">
            <div>
                <h5>
                    جستجوی خودرو
                </h5>
            </div>
            <div class=" form-row">
                <div class="col-sm-8">
                    <label class="sr-only" asp-for="SearchModel.CarName"></label>
                    <input class="form-control" asp-for="SearchModel.CarName" placeholder="نام خودرو">
                </div>
                <div class="d-flex justify-content-center justify-content-sm-end col-sm-4">
                    <button type="submit" class="">
                        جستجو
                    </button>
                </div>
            </div>
        </form>
    </div>
</section>

@await Component.InvokeAsync("LatestCars")

@await Component.InvokeAsync("LatestBlogs")

@await Component.InvokeAsync("About")